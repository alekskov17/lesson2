import { NotAvatarPipe } from './not-avatar.pipe';

describe('NotAvatarPipe', () => {
  it('create an instance', () => {
    const pipe = new NotAvatarPipe();
    expect(pipe).toBeTruthy();
  });
});
